package demo.security.servlet;

public class Sanitize {
    // This method sanitizes SQL input by removing potentially dangerous characters such as single quotes, double quotes, and semicolons.
    public static String sanitizeSQL(String input) {
        return input;
    }
}
